spring:
  application:
    name: cart
  cloud:
    kubernetes:
      reload:
#        mode: event
        enabled: true
        mode: polling
        period: 5000
        monitoring-config-maps: true
      config:
        sources:
          - name: ${spring.application.name}-configmap
